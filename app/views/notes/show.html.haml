.wrapper_with_padding
	#note_show
		%h1.title= @note.title
		%p= simple_format(@note.content)

		.buttons
			%button= link_to "Back", root_path
			%button= link_to "Edit", edit_note_path(@note)
			%button= link_to "Delete", note_path(@note), method: :delete, data: { confirm: "Are you sure?" }